# AWS Production Environment Configuration
# Copy this file to .env.aws and update with your actual values

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# AWS Configuration
AWS_REGION=us-west-2
ENVIRONMENT=prod

# Database Configuration (managed by Terraform/DocumentDB)
# These will be automatically configured in ECS task definitions
# MONGODB_URI=mongodb://admin:password@docdb-cluster:27017/intelliops?ssl=true
# DATABASE_NAME=intelliops

# JWT Configuration (managed by AWS Systems Manager Parameter Store)
# JWT_SECRET=auto-generated-secure-secret
# JWT_EXPIRES_IN=24h

# Server Configuration
PORT=8080
GIN_MODE=release

# AI Configuration
AI_PROVIDER=openai
OPENAI_MODEL=gpt-3.5-turbo

# CORS Configuration (will be set to ALB DNS name)
# CORS_ORIGIN=http://your-alb-dns-name

# Terraform Variables (optional overrides)
TF_VAR_aws_region=us-west-2
TF_VAR_project_name=intelliops-ai-copilot
TF_VAR_environment=prod
TF_VAR_openai_api_key=your-openai-api-key-here

# Optional: Custom domain configuration
# TF_VAR_domain_name=your-domain.com
